"""Package for message handlers.

This subpackage contains the individual router modules used by the bot.  The
routers are grouped by responsibility: common handlers for user
registration and status, admin handlers for financier-only commands, and
payment handlers for callback queries from inline buttons.
"""
