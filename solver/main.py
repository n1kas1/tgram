# -*- coding: utf-8 -*-
"""
main.py — точка входа проекта (запуск графического интерфейса).

Учебный проект: поиск КОРНЯ уравнения f(x)=0 методами касательных (Ньютона)
и секущих. Функции — линейные комбинации x^k, exp(x), ln(x), sin(x), cos(x).

Файлы по ролям (4 человека):
  function_parser.py   — разбор функции из текста + аналитические производные
  numerical_methods.py — методы касательных и секущих (поиск корня f(x)=0)
  task_generator.py    — генератор задач на поиск корня
  gui.py               — графический интерфейс (PyQt5) + график

Запуск:  python main.py
"""

from gui import main

if __name__ == "__main__":
    main()
